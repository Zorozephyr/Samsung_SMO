Operators:
    They have controllers -> Kube Controller Manager(Forever running loop)
    for{
        observer()
        compares() -> Current state and desired state
        if currentState!=desiredState{
            updates/act()
        }
    }
Very Important: Do not make any change in state if there is not difference between actual and desired state..
The operator should be idempotent
The operator should be stateless

CRD+Controller+Business Logic

Kubernetes has 
1.extensibility(custom-resources(Infinte Api Extensions), operators(BusinessLogic),controllers(Self healing magic))
2.Api First(Everything is an api,CLient libraries, backward compatibility)
3.Plugin-Universe(Networking, Storage, Runtime, Webhooks)
4.Standardization(abstractions, service discovery, config management)

                        ┌─────────────────────────────────┐
                        │        USER / KUBECTL            │
                        │   (Declares Desired State)       │
                        └───────────────┬─────────────────┘
                                        │
                                        ▼
                        ┌─────────────────────────────────┐
                        │         API SERVER               │
                        │   (Stores desired state in etcd) │
                        └───────────────┬─────────────────┘
                                        │
                          ┌─────────────┘
                          ▼
            ┌──────────────────────────────────┐
            │                                  │
            │     CONTROLLER / OPERATOR        │
            │     (Reconciliation Group)       │
            │                                  │
            │  ┌────────────────────────────┐  │
            │  │  1️⃣  OBSERVE / WATCH       │  │
            │  │                            │  │
            │  │  Watch API Server for      │  │
            │  │  changes to resources      │  │
            │  │  (Informers / Watch API)   │  │
            │  └─────────────┬──────────────┘  │
            │                │                 │
            │                ▼                 │
            │  ┌────────────────────────────┐  │
            │  │  2️⃣  DIFF / COMPARE        │  │
            │  │                            │  │
            │  │  Desired State (Spec)      │  │
            │  │       vs                   │  │
            │  │  Actual State (Status)     │  │
            │  │                            │  │
            │  │  Are they the same?        │  │
            │  └──┬──────────────────┬──────┘  │
            │     │                  │         │
            │   YES ✅             NO ❌       │
            │     │                  │         │
            │     ▼                  ▼         │
            │  ┌────────┐  ┌────────────────┐  │
            │  │ No-op  │  │ 3️⃣  ACT        │  │
            │  │ (idle) │  │                │  │
            │  └───┬────┘  │ Take action to │  │
            │      │       │ reconcile:     │  │
            │      │       │                │  │
            │      │       │ • Create Pods  │  │
            │      │       │ • Scale Up/Down│  │
            │      │       │ • Update Config│  │
            │      │       │ • Restart      │  │
            │      │       │ • Delete       │  │
            │      │       └───────┬────────┘  │
            │      │               │           │
            │      │               ▼           │
            │      │    ┌───────────────────┐  │
            │      │    │ 4️⃣  UPDATE STATUS │  │
            │      │    │                   │  │
            │      │    │ Report current    │  │
            │      │    │ status back to    │  │
            │      │    │ API Server        │  │
            │      │    └────────┬──────────┘  │
            │      │             │             │
            │      └──────┬──────┘             │
            │             │                    │
            │             ▼                    │
            │  ┌────────────────────────────┐  │
            │  │  5️⃣  RE-QUEUE / RETRY      │  │
            │  │                            │  │
            │  │  Wait for next event or    │  │
            │  │  periodic resync, then     │  │
            │  │  loop back to OBSERVE      │  │
            │  └─────────────┬──────────────┘  │
            │                │                 │
            └────────────────┼─────────────────┘
                             │
                             └────────── 🔄 (Back to Step 1)